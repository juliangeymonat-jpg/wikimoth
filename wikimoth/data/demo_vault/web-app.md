# web-app

The web-app is the customer-facing React frontend. It is built by the
[[frontend-team]] and uses the [[design-system]] component library.

Static assets are served from [[cdn-edge]]. The app talks to backend services
over the public API gateway.
