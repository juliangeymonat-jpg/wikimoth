name: release

# Tag-triggered release. The wheel is built and the documented surface is run
# against it in a clean venv (console script, MCP handshake, version sync)
# BEFORE publish, so a broken artifact fails the release instead of shipping.
#
# Publish uses PyPI Trusted Publishing (OIDC), no stored token. One-time setup:
# pypi.org/project/wikimoth -> Manage -> Publishing -> add a GitHub Actions
# publisher (owner: juliangeymonat-jpg, repo: wikimoth, workflow: release.yml,
# environment: pypi). Until then `build-verify` still runs; only `publish` needs it.

on:
  push:
    tags: ["v*"]

jobs:
  build-verify:
    name: build + released-artifact acceptance
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade pip build
      - name: Build sdist + wheel
        run: python -m build
      - name: Install the built wheel in a CLEAN venv and run the documented surface
        run: |
          set -euo pipefail
          python -m venv /tmp/rel
          /tmp/rel/bin/pip install --upgrade pip
          /tmp/rel/bin/pip install dist/*.whl
          /tmp/rel/bin/wikimoth --version
          /tmp/rel/bin/python -m wikimoth --help
          /tmp/rel/bin/python tests/mcp_acceptance.py
          /tmp/rel/bin/python tests/test_entry_points.py
          /tmp/rel/bin/python tests/test_version_sync.py
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish:
    name: publish to PyPI (trusted publishing)
    needs: build-verify
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
