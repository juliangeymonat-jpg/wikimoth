# design-system

The design-system is the shared component library used by the [[web-app]]. It
defines buttons, forms, and the typography scale.

It is owned by the [[frontend-team]] and versioned independently of the app.
